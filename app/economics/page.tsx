export const metadata = {
  title: "Undrlla Economics — 0% Transaction Fee & Alpha Cohort Manifest",
  description:
    "Deep dive into Undrlla business model: 0.0% GMV fee, 100% setup cashback rebate, API catalog hub streaming, and 20% USDT RevShare.",
};

import EconomicsClient from "@/components/economics-client";

export default function EconomicsPage() {
  return <EconomicsClient />;
}
